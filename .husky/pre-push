# Lightweight quality gate (fast checks only, E2E left to CI)
echo "Running pre-push checks (format, lint, type-check, unit tests)..."
pnpm run check:fast

# Unit tests only (E2E is handled by CI)
echo "Running unit tests..."
pnpm run test:quick
