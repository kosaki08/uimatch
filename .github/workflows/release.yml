name: release
on:
  push:
    tags:
      - 'v*'

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9.15.4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22.21.x'
          registry-url: 'https://registry.npmjs.org'
          cache: 'pnpm'

      - name: Setup Bun (for scripts only)
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Install Playwright browsers
        run: pnpm exec playwright install --with-deps chromium

      - name: Build all packages
        run: pnpm run build

      - name: Run tests
        run: pnpm test

      - name: Pack packages for validation
        run: |
          mkdir -p artifacts
          # Pack all packages in topological order
          pnpm -r --sort pack --pack-destination artifacts

      - name: Upload packed artifacts
        uses: actions/upload-artifact@v4
        with:
          name: npm-packs
          path: artifacts

      # - name: Publish packages to npm
      #   run: |
      #     # Publish all packages in topological order
      #     pnpm -r --sort publish --provenance --access public --no-git-checks
      #   env:
      #     NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
