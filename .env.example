# Figma MCP Server Configuration
FIGMA_MCP_URL=http://localhost:8765
# FIGMA_MCP_TOKEN=your_token_here

# uiMatch / Playwright runtime
# Headless mode (set to false for debugging)
UIMATCH_HEADLESS=true
# Chrome channel (use 'chrome' for consistent rendering)
# UIMATCH_CHROME_CHANNEL=chrome
# HTTP timeout in milliseconds
UIMATCH_HTTP_TIMEOUT_MS=20000

# Optional: Basic auth for target URLs (e.g., staging environments)
# BASIC_AUTH_USER=username
# BASIC_AUTH_PASS=password
