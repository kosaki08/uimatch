# Figma MCP Server Configuration
# Default: Figma Desktop App MCP server (starts automatically when Figma is open)
FIGMA_MCP_URL=http://127.0.0.1:3845/mcp
# FIGMA_MCP_TOKEN=your_token_here

# Figma REST fallback (no MCP required for image fetching)
# Get your Personal Access Token from https://www.figma.com/
FIGMA_ACCESS_TOKEN=your_token_here

# Optional: Direct base64 PNG bypass (for Claude integration)
# Accepts Figma PNG as base64 string (data:image/png;base64, prefix is optional)
# UIMATCH_FIGMA_PNG_B64=

# uiMatch / Playwright runtime
# Headless mode (set to false for debugging)
UIMATCH_HEADLESS=true
# Chrome channel (use 'chrome' for consistent rendering)
# UIMATCH_CHROME_CHANNEL=chrome
# HTTP timeout in milliseconds
UIMATCH_HTTP_TIMEOUT_MS=20000

# Optional: Basic auth for target URLs (e.g., staging environments)
# BASIC_AUTH_USER=username
# BASIC_AUTH_PASS=password
